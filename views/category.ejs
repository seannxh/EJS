<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category %> - <%= restaurant.name %></title>
</head>
<body>
  <h1><%= category %> Menu</h1>
  <%- include('./partials/nav') %>
  <ul>
    <% menuItems.forEach(item => { %>
        <li>
            <strong><%= item.name %></strong><% if (item.rating === 5) { %>⭐<% } %>: $<%= item.price %><br>
            <em>Rating: <%= item.rating %>/5</em><br>
            <p><%= item.details %></p>
        </li>
    <% }) %>
</ul>

</body>
</html>
